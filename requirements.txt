# All versions below were resolved by pip's dependency resolver together
# (not guessed/hand-picked) and verified with `pip check` + a full test run
# on 2026-08-17. If you hit a conflict anyway, delete the pins and run:
#   pip install -r requirements.txt --no-deps  (then pip check)
# or simply remove version pins and let pip resolve fresh — pin ranges churn
# fast in the LangChain ecosystem.

langchain==1.3.15
langchain-community==0.4.2
langchain-core==1.5.5
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langchain-chroma==1.1.0
langchain-groq==1.1.3
groq==0.37.1
chromadb==1.5.9
sentence-transformers==5.7.0

fastapi==0.141.1
uvicorn==0.52.3
streamlit==1.61.1
python-dotenv==1.2.2
pydantic==2.13.4

# Guardrails
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy==3.8.15
# Presidio needs this spaCy model for PII detection. Pinned as a direct wheel
# URL (not `python -m spacy download`) because platforms like Streamlit
# Community Cloud install requirements.txt in one pass, then lock the
# environment — a separate download step at import time fails there with a
# permission error. Installing it here makes it part of the same pip pass.
en_core_web_lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# Observability
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
prometheus-client==0.26.0

# Evals
ragas==0.4.3
datasets==5.0.1
pandas==3.0.2

# Dev / CI
pytest==9.1.1
ruff==0.16.3